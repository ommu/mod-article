article_submenu:
  view:
    label: Detail
    url:
      0: 'admin/view'
      id: $_GET*id
    select: action
  update:
    label: Update
    url:
      0: 'admin/update'
      id: $_GET*id
    select: action
  photo:
    label: Photo(s)
    url:
      0: 'o/image/manage'
      id: $_GET*id
    select: controller
  document:
    label: Document(s)
    url:
      0: 'o/file/manage'
      id: $_GET*id
    select: controller
  delete:
    label: Delete
    url:
      0: 'admin/delete'
      id: $_GET*id
    htmlOptions:
      data-confirm: Are you sure you want to delete this item?
      data-method: post
    select: action
setting_submenu:
  update:
    label: Settings
    url: ['setting/admin/update']
    select: controller
  category:
    label: Categories
    url: ['setting/category']
    select: controller